import assert from 'assert';
import { readline, composeSolution, lines } from '../solution/';



describe('Basic Mocha Test', () => {
  it('2 should equal 2', () => {
    assert.equal(2, 2);
  })
});
